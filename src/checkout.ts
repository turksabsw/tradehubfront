import './style.css';

// Checkout page entry point - components will be added in subsequent subtasks
const app = document.getElementById('app');
if (app) {
    app.innerHTML = '<div class="checkout-page"></div>';
}
