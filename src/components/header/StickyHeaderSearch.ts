/**
 * StickyHeaderSearch
 * Handles expand/collapse behavior for the compact header search bar.
 * Now driven by Alpine.js x-data="stickyHeaderSearch" in the template.
 * This file retains a no-op export for backward compatibility with page entry files.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-function
export function initStickyHeaderSearch(): void {}
